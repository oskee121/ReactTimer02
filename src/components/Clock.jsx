var React = require('react');
var PropTypes = React.PropTypes;

var Clock = React.createClass({

    render: function() {
        return (
            <div />
        );
    }

});

module.exports = Clock;
